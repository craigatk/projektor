plugins {
    id 'jacoco'
}

apply from: "../../../gradle/kotlin.gradle"

dependencies {
    api(project(":server:parsing:junit-results-parser"))

    testImplementation(project(':server:test:test-fixtures'))
}
jacocoTestReport {
    reports {
        xml.enabled true
    }
}
